# python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/

# data + artifacts (live in GCS, not git — see GCP_COMPUTE_PLAN)
# NB: leading slash = ROOT ONLY, so these never match the source package patchguard/data/.
/data/
/results/
/runs/
/checkpoints/
*.ckpt
*.pt
*.safetensors
wandb/

# os
.DS_Store
*.png

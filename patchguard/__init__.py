"""PatchGuard — embedding inversion of visual document retrievers, and a patch-scoped defense.

See docs/MASTER_PLAN.md for the 0->100 build order. The public surface is intentionally small;
import submodules directly (patchguard.data.align, patchguard.eval.frontier, ...).
"""

__version__ = "0.0.1"
